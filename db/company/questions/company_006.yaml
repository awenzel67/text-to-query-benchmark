category: []
database: company
js_ref: var count_projects=0;data.employees.map(employee => { count_projects=count_projects
  + employee.profile.projects.length });var result=count_projects;
question: What is the count of all projects.
sql_ref: select count(*) from projects
