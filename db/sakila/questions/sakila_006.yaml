category: [filter,projection]
database: sakila
question: In wich films a Drama occurs?
js_ref: |  
    let result=data.films.filter(film=>film.Description.includes('Drama'))
    .map(film=>{return film.Title})
sql_ref: |
    select title from film
    where description like '%Drama%';    